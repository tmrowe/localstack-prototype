# These properties are used when running tests.

# Spring Configuration:
cloud.aws.region.static=us-east-1
cloud.aws.stack.auto=false

# Service URls:
s3.url=http://localhost:4572
kinesis.url=http://localhost:4568
lambda.url=http://localhost:4574

# S3 Configuration:
s3.event.persistence.bucket=d6-rpc-events
s3.request.bucket=d6-rpc-requests

# Kinesis Configuration:
kinesis.request.stream.name=request_stream_name
kinesis.shard.count=5

# TODO: Replace lambdaJarPath with an artifact pulled by Maven.
# Lambda Configuration:
lambda.s3.code.bucket=d6-lambda-code
lambda.s3.key=lambda
lambda.jar.path=/Users/thomas/Workspace/lambda-prototype/target/lambda-prototype-1.0.0-jar-with-dependencies.jar
lambda.function.name=HelloWord
lambda.runtime=java8
lambda.role=arn:aws:iam::123456789012:role/lambda-role
lambda.handler=com.lambda.prototype.basic.HelloWorld
lambda.description=Example hello world function
lambda.publish=true
lambda.timeout=20
lambda.memory.size=1024
